%{
  deps: %{
    blitz: %{
      hex: "~> 0.3.0",
      default_order: [:hex],
      publish_order: [:hex]
    },
    weld: %{
      hex: "~> 0.8.4",
      default_order: [:hex],
      publish_order: [:hex]
    }
  }
}
